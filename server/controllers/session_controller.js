'use strict';

exports.logout = function(req, res) {
  //var username = (req.session && req.session.user) ? req.session.user.username : '';
  //req.session.destroy(function(err) {
  //  if (err) {
  //    logger.info(username ? 'Unable to destroy session for user ' + username : 'Unable to destroy session');
  //  } else {
  //    logger.debug(username ? 'User ' + username + ' has been logged out.' : 'Session has been destroyed');
  //  }
  //});
  //req.session = null;
  //res.redirect(username ? r.logout_path() : r.home_path());
};
